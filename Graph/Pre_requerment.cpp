#include <bits/stdc++.h>
using namespace std;

/*
    ## Recursion ##
--------------------------
return_type Func(Parameters) {
    // 1. base case
    if(base_condition) {
        return base_value;
    }

    // 2. Recursive call on smaller input
    return Func(smaller_input);
}

    ## vector ##
--------------------------
    v.push_back(x);   // O(1)
    v.pop_back();     // O(1)
    
    ## stack ##
---------------------------
    st.push(x);      // O(1)
    st.top();        // O(1)
    st.pop();        // O(1)

    ## queue ##
--------------------------
    q.push(x);      // O(1)
    q.front();      // O(1)
    q.back();       // O(1)
    q.pop();        // O(1)

    ## priority_queue ##
-------------------------------
    priority_queue<int> pq; // Max Heap [parent >= child]
    priority_queue<int, vector<int>, greater<int>> pq; // Min Heap [parent <= child]

    pq.push(x);     // O(log n)
    pq.pop();       // O(log n)
    pq.top();       // O(1)

*/
int main() {

}
